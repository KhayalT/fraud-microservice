server:
  port: 8000

spring:
  application:
    name: customer
  datasource:
    username: khayal
    password: password
    url: jdbc:postgresql://localhost@host.docker.internal:5432/fraud-managment
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true